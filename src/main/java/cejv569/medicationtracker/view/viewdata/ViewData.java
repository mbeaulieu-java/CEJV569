package cejv569.medicationtracker.view.viewdata;

/**
 * The ViewData is a base class for data objects which are used by the view controller classes
 * to initialize controls on their respective fxml forms. The ViewData classes are very light
 * classes and contain data that is fairly custom to each view they serve.  Hence, for now this
 * class does not do much.
 */
public abstract class ViewData {
}
