package cejv569.medicationtracker.model.transactioninterfaces;

/**
 *  The DataTransaction interface class is the base class for all the transaction interfaces, which are
 *  the interfaces used by the ViewOperation subclasses to interface with the database layer classes
 *  (DataTransactions subclasses).  The DataTransactions subclasses implement all DataTransaction
 *  interfaces. Hence, these interfaces are the communication layer between the model layer and the
 *  database.
 */
public interface DataTransaction {
}
